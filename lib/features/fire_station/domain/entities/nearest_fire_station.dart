class NearestFireStation {
  const NearestFireStation({
    required this.id,
    required this.name,
    required this.distance,
  });

  final int id;
  final String name;
  final double distance;
}
