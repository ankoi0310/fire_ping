import 'package:flutter/material.dart';

class StationMemberPage extends StatelessWidget {
  const StationMemberPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
